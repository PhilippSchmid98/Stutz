/// Payment intervals for income sources and expense nodes.
enum PaymentInterval { monthly, yearly }

/// Classification of expense nodes.
enum ExpenseType { fixed, variable }

/// Grouping of income sources.
enum IncomeGroup { main, additional }
